The database I'm using for this course is caled dvdrental. So you can login by doing:

psql -d dvdrental

If you want to see the list of databases created in your local postgres, run psql -d postgres -c '\l'
